---
- name: 環境プロビジョニング
  hosts: localhost
  gather_facts: true
  become: false

  roles:
    - role: mise
    - role: homebrew
      when: ansible_os_family == 'Darwin'
    - role: mas
      when: ansible_os_family == 'Darwin'
    - role: mac-setting
      when: ansible_os_family == 'Darwin'
    - role: chezmoi
