---
- name: ワークステーションプロビジョニング
  hosts: localhost
  gather_facts: true
  become: false

  roles:
    # macOS専用ロール
    - role: homebrew
      when: ansible_os_family == 'Darwin'
    - role: mas
      when: ansible_os_family == 'Darwin'
    - role: mac-setting
      when: ansible_os_family == 'Darwin'

    # OS共通ロール
    - role: mise

    # dotfiles管理
    - role: chezmoi
